{
  // Use Expo's recommended base config for React Native projects
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    // Keep everything else from Expo, just tighten a few local options
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
